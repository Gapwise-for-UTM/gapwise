comment on table public.mail_thread_state is 'Private operator-only mailbox organization state; accessed only through service-role Edge Functions after operator authorization.';
comment on table public.mail_drafts is 'Private operator-only server-side email drafts; accessed only through service-role Edge Functions after operator authorization.';
